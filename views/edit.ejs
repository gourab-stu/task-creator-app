<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="https://res.cloudinary.com/dvkmi9cmw/image/upload/v1738417349/task-creator-app-db/favicon.ico">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title>Task Creator and Manager</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="flex w-40 justify-between">
        <a href="/" class="text-blue-400 hover:text-blue-400/80 inline-block mb-10">Go back home</a>
        <a href="/delete/<%= task._id %>" class="text-red-400 hover:text-red-400/80 inline-block mb-10" onclick="return confirm('Do you want to delete this task?')">Delete</a>
      </div>
      <h3 class="text-white text-2xl mb-5">Edit Task</h3>
      <form action="/update/<%= task._id %>" method="post">
        <input type="text" class="block w-full rounded-md outline-none mt-2 px-5 py-2 bg-zinc-800 shadow-md" placeholder="Edit title" name="newTitle" value="<%= task.title %>" required="true" />
        <textarea class="block w-full rounded-md outline-none mt-2 px-5 py-2 resize-none bg-zinc-800 shadow-md" placeholder="Edit your task details" name="newDetails" required="true"><%= task.details %></textarea>
        <div class="flex justify-between my-5">
          <h3 class="text-sm text-zinc-600 inline-block">Created at <%= task.createdAt.toLocaleString("en-US", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit", second: "2-digit" }); %></h3>
          <h3 class="text-sm text-zinc-600 inline-block">Updated at <%= task.updatedAt.toLocaleString("en-US", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit", second: "2-digit" }); %></h3>
        </div>
        <button type="submit" class="mt-2 bg-indigo-500/50 hover:bg-indigo-500/40 cursor-pointer px-5 py-2 rounded-md shadow-md">Create Task</button>
      </form>
    </div>
  </body>
</html>
