<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="https://res.cloudinary.com/dvkmi9cmw/image/upload/v1738417349/task-creator-app-db/favicon.ico">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title><%= task.title %> | Task Creator and Manager</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="flex w-60 justify-between">
        <a href="/" class="text-blue-400 hover:text-blue-400/80 inline-block mb-10">Go back home</a>
        <a href="/edit/<%= task._id %>" class="text-blue-400 hover:text-blue-400/80 inline-block mb-10">Edit</a>
        <a href="/delete/<%= task._id %>" class="text-red-400 hover:text-red-400/80 inline-block mb-10" onclick="return confirm('Do you want to delete this task?')">Delete</a>
      </div>
      <div class="text-3xl tracking-tight text-gray-200"><%= task.title %></div>
      <div class="mt-5 font-light text-gray-300"><%= task.details %></div>
      <div class="flex justify-between my-10">
        <h3 class="text-sm text-zinc-600 inline-block">Created at <%= task.createdAt ? task.createdAt.toLocaleString("en-US", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit", second: "2-digit" }) : "unknown" %></h3>
        <h3 class="text-sm text-zinc-600 inline-block">Updated at <%= task.updatedAt ? task.updatedAt.toLocaleString("en-US", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit", second: "2-digit" }) : "unknown" %></h3>
      </div>
    </div>
  </body>
</html>
